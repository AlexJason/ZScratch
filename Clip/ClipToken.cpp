#include "ClipToken.h"

std::vector<std::vector<std::string>> ClipAPI GetWordList(std::istream f) {
	std::vector<std::vector<std::string>> ret(0);
	std::string strin;
	while (!f.eof()) {
		f >> strin;

	}
	return ret;
}
